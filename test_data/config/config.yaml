multi_species:
  panel_fasta: "test_data/resources/panel/amplicon_panel.fasta"

nanofilt:
  enable: false
  min_len: 200
  max_len: 0
  min_q: 9

cluster:
  enable: true
  id_threshold: 0.995

consensus:
  racon_rounds: 1  # Reduced for faster testing
  medaka_model: "r1041_e82_400bps_hac_v4.2.0"

identify:
  min_pident: 0.90  # Relaxed for test data
  min_qcov: 0.80    # Relaxed for test data
  top_delta: 0.01

threads:
  map: 2       # Reduced threads for testing
  polish: 2    # Reduced threads for testing
  mmseqs: 2    # Reduced threads for testing
